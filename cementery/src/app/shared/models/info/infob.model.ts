export interface InfoFilter {
    title?: string;                // Título de la información
    category?: string;             // Categoría de la información
    features?: string;             // Características incluidas
    exclusions?: string;           // Exclusiones
}
