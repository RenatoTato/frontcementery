export interface GuiaFilter {
    title?: string;                // Título de la guía
    category?: string;             // Categoría de la guía
    description_short?: string;    // Descripción corta de la guía
}
